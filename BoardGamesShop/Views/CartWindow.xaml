<Window x:Class="BoardGamesShop.Views.CartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Košík" Height="420" Width="600" Background="#FFFBEF">
    <DockPanel Margin="10">
        <TextBlock x:Name="TotalText" DockPanel.Dock="Bottom"
                   FontWeight="Bold" FontSize="16" Margin="0,8,0,0"/>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Zavřít"
                    Width="100" Margin="0,0,8,0"
                    Click="Close_Click"
                    Background="#FF6C6C" Foreground="White"
                    FontWeight="Bold" FontSize="12"
                    Padding="5"
                    BorderThickness="0" Cursor="Hand"
                    HorizontalAlignment="Stretch" BorderBrush="#FF6C6C" Height="30"/>
            <Button x:Name="BuyButton" Content="Koupit"
                    Width="100"
                    Click="BuyButton_Click"
                    Padding="10,4"
                    Background="#4FC0CE" Foreground="White"
                    FontWeight="Bold" FontSize="12"
                    BorderThickness="0" Cursor="Hand"
                    HorizontalAlignment="Stretch" BorderBrush="#4FC0CE" Height="30"/>
        </StackPanel>

        <DataGrid x:Name="Grid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Hra" Binding="{Binding Game.Name}" Width="*"/>
                <DataGridTextColumn Header="Cena/ks" Binding="{Binding UnitPrice}" Width="90"/>
                <DataGridTemplateColumn Header="Počet" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="−" Width="24" Margin="0,0,4,0" Click="Dec_Click"/>
                                <TextBlock Text="{Binding Quantity}" Width="30" TextAlignment="Center"/>
                                <Button Content="+" Width="24" Margin="4,0,0,0" Click="Inc_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Součet" Binding="{Binding LineTotal}" Width="90"/>
                <DataGridTemplateColumn Header="Akce" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Smazat" Click="Remove_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
